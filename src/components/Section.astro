---
const { title } = Astro.props;
---

<section>
  {title && <h2>{title}</h2>}
  <slot />
  <!--  El slot es lo que le llega al componente como contenido -->
</section>

<style>
  section {
    max-width: 700px;
    margin: 0 auto 48px;
    text-align: ju;
  }

  h2 {
    margin-bottom: 8px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 1.5rem;
  }
</style>
